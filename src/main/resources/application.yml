spring:
  profiles:
    active: default
  http:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mail:
    host: smtp.ukr.net
    port: 465
    username: GreatStartSupport@ukr.net
    password: metaPassword
    properties:
      mail:
        smptp:
          ssl:
            enable: true
    test-connection: false
  messages:
    basename: MessagesBundle
support:
  email: GreatStartSupport@ukr.net

---
spring:
  profiles: prod
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: root
  jpa:
    properties:
      hibernate:
        default_schema: startup_platform
    hibernate:
      ddl-auto: update
security:
  basic:
    enabled: false
server:
  port: 8080
---
spring:
  profiles: default
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        default_schema: startup_platform
server:
  port: 9001

